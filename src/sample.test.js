import { describe, it, expect, test } from 'vitest';

describe('Setup', () => {
  it('should work', () => {
    expect(1 + 1).toBe(2);
  });
  test('true is true', () => {
    expect(true).toBe(true);
  });
});
